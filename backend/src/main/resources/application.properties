# Server port
server.port=8080

# MongoDB connection (adjust as needed)
spring.data.mongodb.uri=mongodb://localhost:27017/spamdetectdb

# Enable CORS for frontend Vite dev server
# (Alternatively, handled in controller with @CrossOrigin)
